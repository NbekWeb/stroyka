import 'package:flutter/material.dart';

class ScrollableContentStyles {
  // Default padding
  static const EdgeInsets defaultPadding = EdgeInsets.symmetric(horizontal: 20);
  
  // Default physics
  static const ScrollPhysics defaultPhysics = AlwaysScrollableScrollPhysics();
  
  // Default values
  static const bool defaultReverse = false;
} 